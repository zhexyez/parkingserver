// Core v.1
/*

	This is a system for a parking network.

	-> Every folder contains:
		|	database of users
		|	initialization file
		|	mandatory files for processing
		|	microservice for removing unactive users

	-> ParkingSystem folder contains:
		|	database of parkings
		|	initialization file
		|	mandatory files for processing

	-> Core:
		|	microservice for adding/removing parkings
		|	features

*/

const fs = require("fs")
